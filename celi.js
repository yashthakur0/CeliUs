
 (function ( $ ) {
 
    $.fn.celi = function( options ) {
        let value = parseInt($(this).val());
        return  value ;    
        // let cuberoot = Math.cbrt(value)
        // let cuberoot = Math.random(value)
        // let cuberoot = Math.ceil(value)
        // let cuberoot = Math.sign(value)
        // let cuberoot = Math.sign(value)
        // return cuberoot;
    };
  }( jQuery ));
  
  
      